{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run circular orbit",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/tests/circular_orbit.py",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "console": "integratedTerminal"
    }
  ]
}