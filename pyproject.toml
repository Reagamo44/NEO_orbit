
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neo-orbit-determination"
version = "0.1.0"
description = "Orbit propagation and navigation utilities"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]